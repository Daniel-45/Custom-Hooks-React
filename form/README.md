# useForm Hook

Ejemplo de uso:

```
const initialForm = {
    name: '',
    surname: '',
    email: ''
}

const [formValues, handleInputChange, reset] = useForm(initialForm);
``` 

Example of use:

```
const initialForm = {
    name: '',
    surname: '',
    email: ''
}

const [formValues, handleInputChange, reset] = useForm(initialForm);
``` 