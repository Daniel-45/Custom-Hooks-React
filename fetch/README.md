# useFetch Hook

Ejemplo de uso:

```
const url = 'api endpoint';
const { data, loading, error } = useFetch(url);
```

Example of use:

```
const url = 'api endpoint';
const { data, loading, error } = useFetch(url);
```